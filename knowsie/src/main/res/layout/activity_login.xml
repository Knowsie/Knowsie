<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/mainBlue"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context="cs499.knowsie.LoginActivity">


    <ImageView
        android:id="@+id/logo"
        android:layout_width="match_parent"
        android:layout_height="406dp"
        android:src="@drawable/logo_knowsie"/>

    <Button
        android:id="@+id/twit_login_button"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/logo"
        android:layout_centerHorizontal="true"
        android:background="#55ACEE"
        android:drawableStart="@drawable/ic_twitter_white"
        android:paddingEnd="40dp"
        android:paddingStart="40dp"
        android:text="@string/twitter_signin"
        android:textColor="#FFFFFF"/>

    <Button
        android:id="@+id/inst_login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/twit_login_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:background="@color/mainBrown"
        android:drawablePadding="8dp"
        android:drawableStart="@drawable/ic_instagram"
        android:paddingEnd="40dp"
        android:paddingStart="40dp"
        android:text="@string/instagram_signin"
        android:textColor="#FFFFFF"/>

</RelativeLayout>
